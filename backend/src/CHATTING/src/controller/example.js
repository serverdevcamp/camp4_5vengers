module.exports = {
    create : (a, b, c) => {
        return new Promise(async(resolve, reject) => {
            // if("실패"){
            //     resolve({
            //         code : statusCode.OK,
            //         json : util.successFalse(statusCode.HOME_SHOW_FAIL, resMessage.X_READ_FAIL(home))
            //     });
            //     return;
            // }
            // resolve({
            //     code : statusCode.OK,
            //     json : util.successTrue(statusCode.HOME_SHOW_FAIL, resMessage.X_READ_FAIL(home))
            // });
            // return;
        });
    },
    read : () => {

    }, 
    readAll : () => {

    },
    update : () => {

    },
    delete : () => {

    }
}